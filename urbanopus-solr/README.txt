Installation
------------

Drop the collection1 folder into {solr-install}/examples/solr 

Then add the core to the solr instance using the web UI.

Open the core (may need page refresh) and then use the DataImport module.